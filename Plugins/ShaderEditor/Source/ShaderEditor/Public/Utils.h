#pragma once

#include "Core.h"
#include "SlateBasics.h"

namespace Utils
{
	const TAttribute<FSlateColor> Yellow(FSlateColor(FLinearColor(1.0, 1.0, 0.0)));
	const TAttribute<FSlateColor> Green(FSlateColor(FLinearColor(0.0, 1.0, 0.0)));
	const TAttribute<FSlateColor> White(FSlateColor(FLinearColor(1.0, 1.0, 1.0)));
	const TAttribute<FSlateColor> Black(FSlateColor(FLinearColor(0.0, 0.0, 0.0)));
}
